config {
  path = ""
  path = ${?VALUE}

  gatling {
    core {
      runDescription = "VertX Demo"
      runDescription = ${?VALUE}
    }
  }

  simulations {
    baseUrl = "https://localhost:8080"
    baseUrl = ${?VALUE}
    
    maxConnectionPerHost = 64
    maxConnectionPerHost = ${?VALUE}
  }
  

}
